#ifndef STANDALONE_UTILS_H
#define STANDALONE_UTILS_H

#include <string>

using namespace std;

class Utils
{
public:
    static bool iequals(const string &a, const string &b);
};

#endif //STANDALONE_UTILS_H
